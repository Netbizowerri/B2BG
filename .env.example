# Canonical site URL used for generated metadata tags.
VITE_SITE_URL="https://b2bg.org"

# Server-only secret example. Do not expose this in frontend bundles.
GEMINI_API_KEY="REPLACE_WITH_SERVER_SIDE_SECRET"
